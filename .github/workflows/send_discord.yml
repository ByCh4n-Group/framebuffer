name: Test For Discord Push

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Access The Code
      uses: actions/checkout@v2

    - name: Update Submodule
      run: |
        git submodule update --init --recursive

    - name: Execute the test
      run: |
        bash test.sh push
